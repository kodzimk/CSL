int main()
{

  int a = 1;
   printf(a);
   int b[4];
   b[0] = (2 * ((2 + 1) + 2));
   printf(b);


  if((b + 1 + (2*2)> 1 | 2 > 1 & 2 > 1) > 1 > 2 & (2 > 1 & 2 > 1))
  {
   int c[4];
   a = a + 2;
   printf(a);
  }

    char symbol = ' ';
    cin(symbol);
    printf(symbol$n);

    
  
  return 1;
}