int main()
{

  int a = 1;
   printf(a);
  int b[5];
   b[1] = b[1] + 3;
   b[1] = 3;

  if((b[1] - 1) > 0)
  {
  int c[5];
  c[0] = 12;
   printf(c);
   a = a + 2;
   printf(a);
  }
    
     int c[5];
    char symbol = ' ';
    cin(symbol);
    printf(symbol$n);

    
  
  return 1;
}